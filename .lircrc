begin
  button = KEY_UP
  prog = pylirc
  config = up
  repeat = 0
end

begin
  button = KEY_DOWN
  prog = pylirc
  config = down
  repeat = 0
end

begin
  button = KEY_LEFT
  prog = pylirc
  config = left
  repeat = 0
end

begin
  button = KEY_RIGHT
  prog = pylirc
  config = right
  repeat = 0
end

begin
  button = KEY_ENTER
  prog = pylirc
  config = enter
  repeat = 1
end
